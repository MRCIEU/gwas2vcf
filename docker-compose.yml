version: "2"
services:
  gwas_harmonisation:
    container_name: gwas_harmonisation
    build:
      context: .
      dockerfile: Dockerfile
    volumes:
      - /data/bgc:/data
      - /data/bgc/ref:/data/ref
